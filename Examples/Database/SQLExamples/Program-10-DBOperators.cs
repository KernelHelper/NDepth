namespace NDepth.Examples.Database.SQLExamples
{
    partial class Program
    {
        /*
        static void SqlCreateDbExample(Database db)
        {
            Console.WriteLine("=== " + MethodInfo.GetCurrentMethod().Name + " ==="); 
          
            // Create new MyDatabase database.
            Console.WriteLine("Query returns: " + db.ExecuteNonQuery("CREATE DATABASE MyDatabase;"));
        }

        static void SqlDropDbExample(Database db)
        {
            Console.WriteLine("=== " + MethodInfo.GetCurrentMethod().Name + " ==="); 
          
            // Drop new MyDatabase database.
            Console.WriteLine("Query returns: " + db.ExecuteNonQuery("DROP DATABASE MyDatabase;"));
        }
        */
    }
}
